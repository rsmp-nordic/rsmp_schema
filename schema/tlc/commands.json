{
  "$comment" : "This part is used for both command requesta and command responses",
  "$comment" : "A request has arg/c0, while a response has rsv/age, but the validation",
  "$comment" : "of these attributes happens only if they are present.",
  "items" : {
    "allOf" : [
      {
        "properties": { "cCI" : { "enum": [
          "M0001",
          "M0002"
        ]}}
      },
      {
        "$comment" : "Bug? unless we require cCI, M0001.json is included even when cCI is not present",
        "if": { "required" : ["cCI"], "properties" : { "cCI": { "const": "M0001" }}},
        "then" : { "$ref" : "M0001.json" }
      },
      {
        "if": { "required" : ["cCI"], "properties" : { "cCI": { "const": "M0002" }}},
        "then" : { "$ref" : "M0002.json" }
      }
    ]
  }
}
